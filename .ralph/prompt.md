OpenSpec-driven development:

- Changes are managed via OpenSpec (openspec/ directory).
- Use openspec CLI to create and manage changes.
- Ralphy orchestrates agent loops using OpenSpec context automatically.
- Progress is appended to .ralph/progress.txt.
- Current status lives in .ralph/status.txt.
