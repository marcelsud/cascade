input:
  redis_list:
    url: "redis://localhost:6380"
    key: "e2e-test-list"
    direction: "left"
    timeout: 5

pipeline:
  processors:
    - log:
        level: "info"
        include_content: true

output:
  capture:
    max_messages: 5
